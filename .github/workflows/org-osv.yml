name: org-osv

on:
  workflow_dispatch:
  schedule:
    - cron: "23 8 25 * *"  # monthly (minute-safe policy)

permissions: read-all

concurrency:
  group: org-osv-${{ github.ref }}
  cancel-in-progress: true

jobs:
  osv:
    uses: gr8monk3ys/github/.github/workflows/reusable-osv.yml@4a306a94d72d10b8d7835d697521a469fc6deab1
