name: org-codeql

on:
  workflow_dispatch:
  schedule:
    - cron: "23 8 25 * *"  # monthly (minute-safe policy)

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: org-codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    uses: gr8monk3ys/github/.github/workflows/reusable-codeql.yml@4a306a94d72d10b8d7835d697521a469fc6deab1
